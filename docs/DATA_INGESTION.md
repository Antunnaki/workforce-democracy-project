# Data Ingestion
- Government APIs: OpenStates (US), plus per-country adapters
- News: allowlist + trust tiers, conditional requests (ETag/Last-Modified)
- AI cache: short TTL for generic Q&A; bills persistent when settled
